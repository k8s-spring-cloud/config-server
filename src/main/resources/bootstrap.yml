---
info:
  component: WB3 - Spring Cloud Configuration Server
spring:
  application:
    name: wb3-k8s-config-server
  cloud:
    config:
      server:
        encrypt:
          enabled: true
        git:
          uri: https://github.com/k8s-spring-cloud/1-Configuration
          search-paths:
            - '*' # Search any directory in git repo with '*'; does not limit you to specific directories
encrypt:
  key: WB3K8sVeryVeryTopSecretKeyToEncryptWith